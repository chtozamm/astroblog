---
const pathname = Astro.url.pathname
---

<nav
  class="my-8 flex select-none flex-wrap items-center justify-center gap-4 text-sm text-neutral-200 sm:my-8">
  <a
    href="/"
    class={`${pathname === "/" && "font-bold text-neutral-50 pointer-events-none"} sm:min-w-16 p-2 link`}
    >Home</a
  >
  <a
    href="/blog"
    class={`${(pathname === "/blog/" || pathname === "/blog") && "font-bold text-neutral-50 pointer-events-none"} sm:min-w-16 p-2 link`}
    >Blog</a
  >
  <a
    href="/about"
    class={`${(pathname === "/about/" || pathname === "/about") && "font-bold text-neutral-50 pointer-events-none"} sm:min-w-16 p-2 link`}
    >About</a
  >
  <a
    href="/case-studies"
    class={`${(pathname === "/case-studies/" || pathname === "/case-studies") && "font-bold text-neutral-50 pointer-events-none"} sm:min-w-16 p-2 link`}
    >Works</a
  >
  <a
    href="/testimonials"
    class={`${(pathname === "/testimonials/" || pathname === "/testimonials") && "font-bold text-neutral-50 pointer-events-none"} sm:min-w-16 p-2 link`}
    >Testimonials</a
  >
</nav>

<style>
  nav {
    text-transform: uppercase;
    font-weight: 600;
  }
</style>
